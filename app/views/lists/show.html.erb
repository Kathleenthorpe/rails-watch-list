<h1><%= @list.name %></h1>
<br>
<br>
  <% @list.movies.each do |movie| %>
<ul>
<li><%= movie.title %></li>
<li><%= movie.overview %></li>
<li><%= image_tag movie.poster_url %></li>
</ul>
<% end %>



<%= link_to "Add a New Movie", new_list_bookmark_path(@list) %>
<%= link_to "Go back to the main menu", lists_path %>
<p></p>
